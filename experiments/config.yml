path:
  train: '../DATASET_INPAINTING/temp_gt/'
  experiment: './experiments/dummy/'

dataset:
  centered: False
  mask_height: 30
  mask_width: 30
  image_height: 128
  image_width: 128
  max_masks_count: 5
  num_workers: 10
  factor: 8

architecture:
  # iteration
  num_blocks: 3

training:
  batch_size: 2           #8 
  iterations: 10
  learning_rate: 0.0001   # set learning rate for training generator and discriminator as 10:1
  beta1: 0                # 0
  beta2: 0.9              # 0.9
  save_iters: 2
  max_iteration: 100

gpu: ''
seed: 1337