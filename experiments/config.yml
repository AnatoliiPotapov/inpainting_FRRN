path:
  train: '/mnt/data1/home/a.s.potapov/data/inpainting/Places365/data_large/'
  test: '/mnt/data1/'
  experiment: './experiments/dummy/'

dataset:
  image_height: 600
  image_width: 500
  max_masks_count: 5
  num_workers: 0
  factor: 8

architecture:
  num_blocks: 8

training:
  batch_size: 1           # 8 
  learning_rate: 0.0001   # set learning rate for training generator and discriminator as 10:1
  beta1: 0                # 0
  beta2: 0.9              # 0.9
  mse_loss_weight: 0.0      # 0
  style_loss_weight: 100  # 100
  rec_loss_weight: 20     # 20
  step_loss_weight: 2     # 2
  # not implemented
  adv_loss_weight: 0.1    # 0.1
  
  save_iters: 500
  max_iteration: 1000000
  tf_summary_iters: 200

gpu: '1' #[0,1,2,3]
seed: 1337
