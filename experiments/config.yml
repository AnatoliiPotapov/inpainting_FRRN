path:
  train: '../Datasets/Huawei/DATASET_INPAINTING/train_gt/'
  experiment: './experiments/dummy/'

dataset:
  centered: True
  mask_height: 50
  mask_width: 50
  image_height: 600
  image_width: 500
  max_masks_count: 1
  num_workers: 10
  factor: 8

architecture:
  checkpoint: ''
  # iteration
  num_blocks: 1

training:
  batch_size: 4           #8 
  iterations: 10
  learning_rate: 0.0001   # set learning rate for training generator and discriminator as 10:1
  beta1: 0                # 0
  beta2: 0.9              # 0.9

gpu: '0'
seed: 1337