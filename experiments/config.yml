path:
  train: '/mnt/data1/home/a.s.potapov/data/inpainting/DATASET_INPAINTING/train_gt/'
  experiment: './experiments/dummy/'

dataset:
  centered: False
  mask_height: 50
  mask_width: 50
  image_height: 600
  image_width: 500
  max_masks_count: 5
  num_workers: 8
  factor: 8

architecture:
  # iteration
  num_blocks: 5

training:
  batch_size: 12          #8 
  learning_rate: 0.0001   # set learning rate for training generator and discriminator as 10:1
  beta1: 0                # 0
  beta2: 0.9              # 0.9
  mse_loss_weight: 1      # 0
  style_loss_weight: 100  # 100
  rec_loss_weight: 20     # 20
  # not implemented
  step_loss_weight: 2     # 2
  adv_loss_weight: 0.1    # 0.1
  
  save_iters: 100
  max_iteration: 1000000

gpu: '0' #[0,1,2,3]
seed: 1337
